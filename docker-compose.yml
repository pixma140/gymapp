services:
  gymapp:
    build: .
    container_name: gymapp
    image: gymapp:latest
    environment:
      - NODE_ENV=production
    ports:
      - "8080:80"
    volumes:
      - ./db:/app/data
    restart: unless-stopped
